configuration:
  timeout: 15s
  slowMotion: 5s
  gherkin_location: "features"

application:
  app_name: "{{.AppName}}"
  app_description: "{{.AppDescription}}"
  app_version: "{{.AppVersion}}"

reporting:
  report_format: "html"
